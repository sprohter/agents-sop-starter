# 路由条目草稿

## 触发词 / 场景

- `<trigger-1>`
- `<trigger-2>`

## 入口文件

`<path-to-entry-file>`

## 适用边界

适用：

- `<scope-1>`

不适用：

- `<out-of-scope-1>`

## 建议写入 routing.md 的表格行

```markdown
| `<trigger-1>`、`<trigger-2>` | `<path-to-entry-file>` | `<note>` |
```

