# <role-name> 岗位卡

status: draft
owner: <team-or-person>
last_updated: <yyyy-mm-dd>

## 1. 岗位定位

一句话说明这个岗位负责什么。

## 2. 触发词

- `<trigger-1>`
- `<trigger-2>`
- `<trigger-3>`

## 3. 接管范围

这个岗位默认处理：

- `<responsibility-1>`
- `<responsibility-2>`

这个岗位默认不处理：

- `<out-of-scope-1>`
- `<out-of-scope-2>`

## 4. 常用 SOP

| 场景 | SOP |
|------|-----|
| `<scenario>` | `../sops/<domain>/<sop-name>.md` |

## 5. 输入要求

执行前通常需要：

- `<input-1>`
- `<input-2>`

## 6. 输出标准

默认输出：

- 结论
- 依据
- 风险
- 下一步

## 7. 风险边界

需要先确认的情况：

- `<risk-1>`
- `<risk-2>`

## 8. 维护规则

- 新增 SOP 后，把链接补到本岗位卡。
- 岗位触发词变化后，同步更新 `../routing.md`。

