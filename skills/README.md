# Public Skill Cards

These examples describe reusable tool workflows in a public-safe way.

They intentionally use placeholders for domains, credentials, base URLs,
project IDs, table IDs, user IDs, group IDs, and any runtime state.
